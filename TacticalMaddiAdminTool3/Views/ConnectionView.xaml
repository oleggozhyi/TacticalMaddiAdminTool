<UserControl x:Class="TacticalMaddiAdminTool.Views.ConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="600
             ">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Content="Environment" HorizontalAlignment="Right"  Margin="5" />
        <TextBox Grid.Column="1" x:Name="Environment" Margin="5" Height="25"></TextBox>
        <Button Width="100" Height="25" HorizontalAlignment="Left"  Content="Connect" Grid.Column="2" Margin="10 5 10 5" x:Name="Connect" Visibility="{Binding IsConnectVisible, Converter={StaticResource BooleanToVisibilityConverter}}"></Button>
        <Button Width="100" Height="25" HorizontalAlignment="Left" Content="Disconnect" Grid.Column="2" Margin="10 5 10 5" x:Name="Disconnect" Visibility="{Binding IsDisconnectVisible, Converter={StaticResource BooleanToVisibilityConverter}}"></Button>
    </Grid>
</UserControl>
